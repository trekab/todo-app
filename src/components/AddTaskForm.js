import React from "react";

const AddTaskForm = () => {
  return (
    <form>
      <input type="checkbox"/>
      <input type="text" placeholder="Create a new todo..." />
    </form>
  );
};

export default AddTaskForm;
